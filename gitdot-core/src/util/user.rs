pub const DEFAULT_USER_README: &str = r#"gitdot is the home for great code.

it's written in rust, fully open-source (Apache 2.0), and has not a single annoying ai feature. \
it's design is heavily inspired by CLIs: everything is a keystroke away and every page loads as fast a cli would (fcp of 100ms).

read about how we built it at: [gitdot.io/designs](https://gitdot.io/designs)
<br>

#### getting around

press `?` any time to see the full shortcut list. a few to start with:

- `;`, `:`, or `⌘K` — open the command bar. type any action by name (`new repo`, `settings`, `logout`, …).
- `,` — settings
- `r` — recent repos
- `p` — fuzzy-find (only available in a repo)

<br>

#### getting started

- fill out your profile (click the 'settings' button in actions on the left)
- create a new repo, or import one from GitHub
- install the [gitdot-cli](#install-cli) to push changes!

and if you have any questions, email [founders@gitdot.io](mailto:founders@gitdot.io)
"#;
